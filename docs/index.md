# Sample Runbook

これは `rundocs.config.ts` を置かず、`./docs` 規約（zero-config）で動く最小サンプルです。

- [Example Route](01-example-route.html)

Plugin/GameSchema を導入するとStateBlockの表示名・アイコン・バリデーションが有効になりますが、
このサンプルはPlugin無しでも StateBlock が破綻なく描画されることを確認するためのものです。
